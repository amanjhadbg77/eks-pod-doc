apiVersion: v1
kind: Service
metadata:
  name: eks-pod-doctor-lb
  namespace: mcp-system
spec:
  selector:
    app: eks-pod-doctor
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  type: LoadBalancer
